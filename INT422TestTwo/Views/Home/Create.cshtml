@model INT422TestTwo.ViewModels.MovieCreateForHttpGet

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>MovieCreateHttpGet</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Title, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Title)
                @Html.ValidationMessageFor(model => model.Title)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.TicketPrice, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.TicketPrice)
                @Html.ValidationMessageFor(model => model.TicketPrice)
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">Director</label>
            <div class="col-md-10">
                @Html.DropDownList("DirectorId", Model.DirectorSelectList)
                @Html.ValidationMessageFor(model => model.DirectorSelectList)
            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">Genres</label>
            <div class="col-md-10">
                @Html.ListBox("GenreId", Model.GenreSelectList)
                @Html.ValidationMessageFor(model => model.GenreSelectList)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
